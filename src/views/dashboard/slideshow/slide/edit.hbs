{{> dashhead}}

<div class="content-wrapper">
    
    <section class="slideshow-options">
    <form action="/dashboard/slideshow/edit/{{id}}/slide/{{slideId}}/" method="post"> 

        </fieldset>

        <fieldset class="slideshow-options__fieldset">
        <legend>Slide metaaaaaa {{duration}} options</legend>

            <label>Slide duration:
                <input name="duration" type="number" value="{{duration}}" placeholder="Duration">sec</input>
            </label>

            <label>Slide order:
                <input name="order" type="number" value="{{order}}" placeholder="Order"></input>
            </label>

            {{#if error}}
                <div class="msg">
                    {{error}}
                </div>
            {{/if}}
                    
        </fieldset>

        <input type="submit" value="Edit slide">

    </form>
    <a href="../"><button>Cancel</button></a>

    </section>

</div>

{{> dashfoot}}
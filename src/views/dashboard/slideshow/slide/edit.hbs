{{> dashhead}}

<div class="content-wrapper">
    
    <section class="slideshow-options">
    <form action="/dashboard/slideshow/new/" method="post"> 

        <fieldset class="slideshow-options__fieldset">
        <legend>Content</legend>

            <p>This resembles the available content</p>
                    
        </fieldset>

        <fieldset class="slideshow-options__fieldset">
        <legend>Slide meta options</legend>

            <label>Slide duration:
                <input name="duration" type="number" placeholder="Duration"></input> sec
            </label>

            <label>Slide order:
                <input name="order" type="number" placeholder="Order"></input>
            </label>

            {{#if error}}
                <div class="msg">
                    {{error}}
                </div>
            {{/if}}
                    
        </fieldset>

        <input type="submit" value="Edit slide">

    </form>
    <a href="../"><button>Cancel</button></a>

    </section>

</div>

{{> dashfoot}}